import { LandingPage } from "@/landingpage/index";

export const metadata = {
  title: "KryptonDrop – Proof-of-Human GPU Drops",
  description:
    "Fair, bot-proof GPU hardware drops powered by World ID and Monad. Only real humans win.",
};

export default function Landing() {
  return <LandingPage />;
}
